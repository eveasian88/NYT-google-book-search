import React, { Component } from "react";

class Saved extends Component {
     
    render(){
        return(
            <div>
                saved
            </div>
        );
    }
}

export default Saved;